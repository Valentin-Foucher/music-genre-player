import Head from 'next/head';

export default function Header() {
    return (
        <Head>
            <title>Music Genre Player</title>
            <meta property="og:title" content="Music Genre Player" key="title" />
        </Head>
    )
}
